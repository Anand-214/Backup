#include<stdio.h>
int main()
{
    int arr[20];
    int num, i,count=0;
    printf("Enter the number upto which to print prime numbers:");
    scanf("%d",&num);
    for ( i=0; i<=num; i++)
    {
	if ( num % i != 0)
	{
	    arr[]={i};
	}
	count++;
    }
    for (i=0; i<=num; i++)
    printf(
}
